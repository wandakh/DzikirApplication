<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wanda.idn.dzikirapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DzikirApp">
        <activity android:name=".ui.detail.DzikirPetangActivity"></activity>
        <activity android:name=".ui.detail.DetailArtikelActivity" />
        <activity android:name=".ui.detail.DzikirPagiActivity" />
        <activity android:name=".ui.DzikirDoaHarianActivity" />
        <activity android:name=".ui.QauliyahActivity" />
        <activity android:name=".ui.DzikirPagiPetangActivity" />
        <activity android:name=".ui.DzikirSetiapSaatActivity" />
        <activity android:name=".DoaHarianActivity" />
        <activity android:name=".QauliyahActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>